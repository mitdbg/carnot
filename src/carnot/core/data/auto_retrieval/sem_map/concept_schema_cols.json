[
  {
    "name": "plant_taxon:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: plant_taxon, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a plant_taxon. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Plants of New Zealand that are extinct or plants from the Ordovician period or plants documented through fossils'; values: [\"New Zealand\"] Example 2 - text chunk: 'Trees of Îles des Saintes or Flora of Martinique'; values: [\"Îles des Saintes\", \"Martinique\"] "
  },
  {
    "name": "book:subject",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: subject) from the text chunk. Return null if the text does not explicitly state a subject for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: '1974 books about the American Revolution'; values: [\"American Revolution\"] Example 2 - text chunk: \"Books about military personal that don't have murder in them\"; values: [\"military personnel\"] "
  },
  {
    "name": "film:genre",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: genre) from the text chunk. Return null if the text does not explicitly state a genre for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: \"1980's police buddy thriller comedy films\"; values: [\"police\", \"buddy\", \"thriller\", \"comedy\"] Example 2 - text chunk: 'Films that are 1988 sci-fi films, based on works by Issac Asimov, or 2000 thriller drama films'; values: [\"science fiction\", \"thriller\", \"drama\"] "
  },
  {
    "name": "film:subject",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: subject) from the text chunk. Return null if the text does not explicitly state a subject for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'what are 1960s dance films or Spanish dance films or Flamenco films'; values: [\"dance\", \"Flamenco\"] Example 2 - text chunk: '2000s psychological horror films about murder and widowhood'; values: [\"murder\", \"widowhood\"] "
  },
  {
    "name": "book:publication_year",
    "type": "int",
    "desc": "Extract only explicit values for (domain: book, facet: publication_year) from the text chunk. Return null if the text does not explicitly state a publication_year for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: '1983 fiction Epistolary novels'; values: [\"1983\"] Example 2 - text chunk: 'British 1814 novels'; values: [\"1814\"] "
  },
  {
    "name": "biological_taxon:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: biological_taxon, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a biological_taxon. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Flora of the Southern United States and Eastern Canada but not Ontario'; values: [\"Southern United States\", \"Eastern Canada\", \"Ontario\"] Example 2 - text chunk: 'Trees of the Windward Islands,Îles des Saintes, or Tabasco'; values: [\"Windward Islands\", \"Îles des Saintes\", \"Tabasco\"] "
  },
  {
    "name": "animal:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: animal, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a animal. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Cenozoic mammals of Asia that are also extinct in North America but not Cenozoic animals of Europe'; values: [\"Asia\", \"Europe\"] Example 2 - text chunk: 'Hispaniola animals that are extinct in North America but not Caribbean arthropods'; values: [\"Hispaniola\", \"Caribbean\"] "
  },
  {
    "name": "plant:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: plant, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a plant. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Nearctic realm plants from Sinaloa, but not from Northeastern Mexico'; values: [\"Sinaloa\", \"Northeastern Mexico\"] Example 2 - text chunk: 'What are Orchids of Honduras or, Orchids of Suriname or, Orchids of Guyana?'; values: [\"Honduras\", \"Suriname\", \"Guyana\"] "
  },
  {
    "name": "organism:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: organism, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a organism. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Birds from the US in 1758 that are also prehistoric reptiles of Asia'; values: [\"United States\", \"Asia\"] Example 2 - text chunk: 'Australasian realm flora, Trees of the Philippines, and Flora of Taiwan'; values: [\"Philippines\", \"Taiwan\"] "
  },
  {
    "name": "animal_taxon:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: animal_taxon, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a animal_taxon. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Australian arthropods from the prehistoric period'; values: [\"Australia\"] Example 2 - text chunk: 'Vulnerable fauna of China that are also mammals of Southeast Asia'; values: [\"China\", \"Southeast Asia\"] "
  },
  {
    "name": "book:author",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: author) from the text chunk. Return null if the text does not explicitly state a author for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'what are some Novels by Ernst Jünger, German magic realism novels, or 1985 German novels?'; values: [\"Ernst Jünger\"] Example 2 - text chunk: 'Ion Idriess novels'; values: [\"Ion Idriess\"] "
  },
  {
    "name": "book:genre",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: genre) from the text chunk. Return null if the text does not explicitly state a genre for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Collaborative non-fiction books that are about countries, but not North America'; values: [\"non-fiction\"] Example 2 - text chunk: 'Novels set in North America about the Paranormal but not horror'; values: [\"horror\"] "
  },
  {
    "name": "film:country_of_origin",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: country_of_origin) from the text chunk. Return null if the text does not explicitly state a country_of_origin for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Polish animated fantasy films,Hungarian independent films, or Austrian comedy-drama films'; values: [\"Poland\", \"Hungary\", \"Austria\"] Example 2 - text chunk: 'Spanish science fiction comedy films or 2000s British animated'; values: [\"Spain\", \"United Kingdom\"] "
  },
  {
    "name": "book:setting",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: setting) from the text chunk. Return null if the text does not explicitly state a setting for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: '1991 Novels set in Iceland'; values: [\"Iceland\"] Example 2 - text chunk: 'Novels set in Vietnam but not about disasters'; values: [\"Vietnam\"] "
  },
  {
    "name": "film:setting_location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: setting_location) from the text chunk. Return null if the text does not explicitly state a setting_location for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Khyber Pakhtunkhwa set films or action war films from Pakistan'; values: [\"Khyber Pakhtunkhwa\"] Example 2 - text chunk: 'Films set in Pittsburgh that are LGBT-related romance'; values: [\"Pittsburgh\"] "
  },
  {
    "name": "book:year",
    "type": "int",
    "desc": "Extract only explicit values for (domain: book, facet: year) from the text chunk. Return null if the text does not explicitly state a year for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Russian 1994 novels'; values: [\"1994\"] Example 2 - text chunk: '2004 jewish american fiction novels'; values: [\"2004\"] "
  },
  {
    "name": "film:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Films that are Documentaries about women in Africa or Rwandan documentary films'; values: [\"Rwanda\"] Example 2 - text chunk: 'South Korean spy action films, South Korean science fiction thriller films, or Films about the National Intelligence Service (South Korea)'; values: [\"South Korea\"] "
  },
  {
    "name": "book:publisher",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: publisher) from the text chunk. Return null if the text does not explicitly state a publisher for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: '1945 Alfred A. Knopf and 2nd-millennium books'; values: [\"Alfred A. Knopf\"] Example 2 - text chunk: 'Canadian books about writers by Alfred A. Knopf'; values: [\"Alfred A. Knopf\"] "
  },
  {
    "name": "film:filming_location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: filming_location) from the text chunk. Return null if the text does not explicitly state a filming_location for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Films shot in Santa Monica California'; values: [\"Santa Monica, California\"] Example 2 - text chunk: 'Science films shot in Hertfordshire about bears'; values: [\"Hertfordshire\"] "
  },
  {
    "name": "film:setting",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: setting) from the text chunk. Return null if the text does not explicitly state a setting for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'what are Independent films that are also Films set in hospitals but not Films set in psychiatric hospitals'; values: [\"hospitals\"] Example 2 - text chunk: 'Films based on Around the World in Eighty Days or set in Sylhet'; values: [\"Sylhet\"] "
  },
  {
    "name": "film:year",
    "type": "int",
    "desc": "Extract only explicit values for (domain: film, facet: year) from the text chunk. Return null if the text does not explicitly state a year for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: '1993 drama erotic films about sexuality'; values: [\"1993\"] Example 2 - text chunk: 'Films that are 1988 sci-fi films, based on works by Issac Asimov, or 2000 thriller drama films'; values: [\"1988\", \"2000\"] "
  },
  {
    "name": "book:setting_location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: setting_location) from the text chunk. Return null if the text does not explicitly state a setting_location for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Fiction books from the 2000 set in Ontario'; values: [\"Ontario\"] Example 2 - text chunk: 'Disaster books about London but not war'; values: [\"London\"] "
  },
  {
    "name": "book:time_period",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: book, facet: time_period) from the text chunk. Return null if the text does not explicitly state a time_period for a book. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Books by Feng Menglong or novels that are Chinese comedy or Ming dynasty'; values: [\"Ming dynasty\"] Example 2 - text chunk: 'Gardening or 1670 books'; values: [\"1670\"] "
  },
  {
    "name": "bird:location",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: bird, facet: location) from the text chunk. Return null if the text does not explicitly state a location for a bird. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Birds of South China that are also from Vietnam but not Yunnan'; values: [\"South China\", \"Vietnam\"] Example 2 - text chunk: 'Birds of South America that are not Fauna of Peru described in 1868'; values: [\"South America\"] "
  },
  {
    "name": "film:setting_time",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: setting_time) from the text chunk. Return null if the text does not explicitly state a setting_time for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Hong Kong action films set in 1948'; values: [\"1948\"] Example 2 - text chunk: 'Futuristic films set in the Southwestern United States about Marines'; values: [\"future\"] "
  },
  {
    "name": "animal:time_period",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: animal, facet: time_period) from the text chunk. Return null if the text does not explicitly state a time_period for a animal. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'what are Miocene animals of Europe that are also Odd-toed ungulates'; values: [\"Miocene\"] Example 2 - text chunk: 'Oligocene animals or Paleogene birds of Oceania or Suliformes'; values: [\"Oligocene\", \"Paleogene\"] "
  },
  {
    "name": "film:release_year",
    "type": "int",
    "desc": "Extract only explicit values for (domain: film, facet: release_year) from the text chunk. Return null if the text does not explicitly state a release_year for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: \"what are some 1913 lost films that aren't dramas\"; values: [\"1913\"] Example 2 - text chunk: '1979 comedy films about the arts'; values: [\"1979\"] "
  },
  {
    "name": "plant:region",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: plant, facet: region) from the text chunk. Return null if the text does not explicitly state a region for a plant. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: 'Trees of the Northwestern United States excluding Flora of Western Canada'; values: [\"Northwestern United States\", \"Western Canada\"] Example 2 - text chunk: 'Flora of the Nearctic realm of Papuasia'; values: [\"Nearctic\", \"Papuasia\"] "
  },
  {
    "name": "film:country",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: country) from the text chunk. Return null if the text does not explicitly state a country for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: '1990s drama films that are Canadian adventure films'; values: [\"Canada\"] Example 2 - text chunk: 'American business films shot in Italy'; values: [\"United States\"] "
  },
  {
    "name": "film:time_period",
    "type": "list[str]",
    "desc": "Extract only explicit values for (domain: film, facet: time_period) from the text chunk. Return null if the text does not explicitly state a time_period for a film. Return standardized, canonical values when the text clearly refers to a known value, using a consistent form across documents. Do not change the meaning. If standardization is uncertain, use the exact text-supported value (or return null if the value itself is uncertain). Return only concise entity-like values (i.e., node-style values suitable for a knowledge graph), not descriptive phrases, clauses, or sentences.If the expected type is list[type] and only one value is present, return it as a single-element list (e.g., [value]). If the expected type is int, return a single numeric value (not a list). An int type means exactly one number or null. A list[str] type means it may return one string (as a single-element list), multiple strings, or null.\n\nThe following examples show you how to extract values for this domain and facet. Example: Example 1 - text chunk: \"1950s children's adventure films or films shot in Bristol\"; values: [\"1950s\"] Example 2 - text chunk: '1980s speculative fiction films that are Romance but not American'; values: [\"1980s\"] "
  }
]