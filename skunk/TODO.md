# Skunk TODO
- Sanity-check retrieval on the rebuilt build_v3 (a couple of dev UIDs, e.g. UID0001 should hand extract ≤4 pages). Old retrieval caches predate the rebuild — don't replay them.
- Human-in-the-loop for visual-understanding (chart-read) questions. We should write a mechanism that has the LLM identify visual understanding questions, and then surfaces a popup for the user to answer
- Data page cleaning -- define a pre-competition pipeline that cleans and prepares the data for a corpus before competition
- Critique system redesign: previous critique system was removed because they caused failures over pedantic issues but did not catch meaningful bugs. We should probably come up with new critique subsystem.
- Integrate with Databricks grading harness
- PageIndex retrieve `as_of` shortcut: a branch with `as_of` pins a single bulletin, so skip ToC pick and return that bulletin's pages directly. Saves ~9 ToC-pick calls per as_of branch.
- PageIndex stray buckets: `_finalize_era` leaves tiny singleton buckets the picker can't reach, and `place` now leaves ~1.5% of pages unfiled after the un-merge rebuild. Fold them into the nearest canonical chapter or one always-scanned look-aside bucket.
- PageIndex build: single cross-stage render queue — enqueue a page for rendering the moment the scan flags it, overlapping CPU-bound rasterization with the still-running scan LLM calls instead of serial prerender.
- LLM per-request timeout for runaway Gemini thinking: `make_genai_client` sets no request timeout, so one stuck call blocks a whole gathered batch. Set `SKUNK_LLM_TIMEOUT_S` (~120-180s) via `HttpOptions(timeout=...)` so runaways raise and hit the existing retry path.
- With QATFD -- retrieval is cheap and parallelizable, so we should be able to always retrieve and only lookup-external on replan?
- As a stopgap -- we are still using full page text instead of just selected block for extraction -- consider trimming further, although the extra text may actually be useful.
