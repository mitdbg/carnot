[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "skunk"
version = "0.0.0"
description = "Skunkworks experiments for the Carnot retrieval stack."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
