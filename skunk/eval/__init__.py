"""Eval harnesses for OfficeQA.

  eval.eval_e2e  — full pipeline; writes predicted vs gold answers to a CSV report.
                   Supports --golden to inject golden pages and isolate extraction.

"""
